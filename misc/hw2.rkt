(define newList '())
(define initlist '(hello my fellow students))

(define (makeList (car initList))
  (cons 


(define (abl initlist)
  (if ((null? (cdr initList))
       (makeList(cdr initList)))
      (else (


